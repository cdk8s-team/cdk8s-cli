[project]
name = "cdk8s-cli"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "constructs~={{ constructs_version }}"
]

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"

